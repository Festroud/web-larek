# Проектная работа "Веб-ларек"
Это учебный проект, созданный в рамках курса Яндекс Практикум. Он предлагает пользователям возможность взаимодействовать с системой для заказа товаров, предназначенных для веб-разработчиков.

## Стек технологий
- **HTML**
- **SCSS**
- **TypeScript**
- **Webpack**

## Структура проекта
- `src/` — исходные файлы проекта
- `src/components/` — папка с JS компонентами
- `src/components/base/` — папка с базовым кодом
- `src/pages/index.html` — HTML-файл главной страницы
- `src/types/index.ts` — файл с типами
- `src/index.ts` — точка входа приложения
- `src/scss/styles.scss` — корневой файл стилей
- `src/utils/constants.ts` — файл с константами
- `src/utils/utils.ts` — файл с утилитами

## Инструкция по сборке и запуску
1. Установить зависимости:
   ```bash
   npm install
   ```
2. Запустить проект в режиме разработки:
   ```bash
   npm run start
   ```
3. Для сборки проекта в продакшен:
   ```bash
   npm run build
   ```
---
## Архитектура проекта Web-Larek
### Базовый код
## Класс `Api`
Класс `Api` предназначен для работы с REST API. Он упрощает выполнение HTTP-запросов, таких как `GET`, `POST`, `PUT`, `DELETE`, и предоставляет единое место для управления базовым URL и настройками запросов.
### Конструктор
**`constructor(baseUrl: string, options: RequestInit = {})`**  
Создает экземпляр API-клиента.
- **Параметры:**
  - `baseUrl` (string): Базовый URL для всех запросов.
  - `options` (RequestInit): Настройки запросов, применяемые по умолчанию. Может включать в себя:
   - `headers`: Заголовки (например, авторизация через `Authorization: Bearer <token>`).
---
### Поля
- **`baseUrl` (string):**  
  Хранит базовый URL, указанный при создании экземпляра класса.
- **`options` (RequestInit):**  
  Содержит настройки запросов, включая заголовки (`headers`) и другие параметры, которые применяются по умолчанию ко всем запросам.
---
### Методы
1. `get(uri: string): Promise<object>`  
Выполняет GET-запрос на указанный путь.

- **Параметры:**
  - `uri` (string): Путь к ресурсу (добавляется к `baseUrl`).

- **Возвращает:**  
  Промис с данными, полученными от сервера.
2. `post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>`
Выполняет POST-запрос (или другой указанный метод) с передачей данных в теле запроса.

- **Параметры:**
`uri (string)`: Путь к ресурсу (добавляется к `baseUrl`).
`data (object)`: Данные, передаваемые в теле запроса (в формате JSON).
`method (ApiPostMethods): HTTP-метод (POST, PUT, DELETE)`. По умолчанию — `POST`.
- **Возвращает:** 
Промис с данными, полученными от сервера.
**Базовый URL:**
Методы автоматически добавляют базовый `URL (baseUrl)` к переданным путям `(uri)`.
**Обработка ответа:**
При успешном запросе возвращает данные в формате JSON.
При ошибке пытается извлечь сообщение об ошибке из ответа или возвращает текст статуса.
**Типы**
`ApiPostMethods`
Тип для указания методов HTTP-запросов:
`type ApiPostMethods = 'POST' | 'PUT' | 'DELETE';`
`ApiListResponse<Type>`
Тип ответа для получения списка элементов:
```type ApiListResponse<Type> = {
  total: number;
  items: Type[];
};
```

## Класс EventEmitter
- реализует паттерн "Emitter" для работы с событиями, позволяя подписываться на события, генерировать их и управлять подписками. Он реализует интерфейс IEvents, который описывает методы для работы с событиями.

**Возможности:**
- Подписка на событие с использованием строки или регулярного выражения.
- Отписка от события.
- Вызов события с передачей данных.
- Подписка на все события с использованием глобального слушателя.
- Сброс всех обработчиков событий.
- Генерация событий с использованием триггера.
**Поля:**
`_events: Map<EventName, Set<Subscriber>>` — карта, где ключи — имена событий или регулярные выражения, а значения — множества подписчиков, которые слушают эти события.
``constructor() {
    this._events = new Map<EventName, Set<Subscriber>>(); 
}``
Конструктор инициализирует пустую карту _events, которая будет хранить все подписки на события.

**Методы:**
1. `on<T extends object>(eventName: EventName, callback: (event: T) => void): void`
Подписывает обработчик `(callback)` на указанное событие ``(eventName)`.
- `eventName:` Имя события или регулярное выражение, которое будет проверяться при вызове события.
- `callback:` Функция, которая будет вызвана при срабатывании события, принимающая объект данных типа T, связанный с событием.

2. `off(eventName: EventName, callback: Subscriber): void`
Удаляет обработчик `(callback)` для указанного события `(eventName)`.
`eventName:` Имя события, с которого нужно снять обработчик.
`callback:` Обработчик, который нужно удалить.

3. `emit<T extends object>(eventName: string, data?: T): void`
Инициирует событие с переданным именем `(eventName)` и данными `(data)`.
`eventName:` Имя события, которое должно быть вызвано.
`data:` Данные, которые могут быть переданы подписчикам при срабатывании события (необязательный параметр).

4. `onAll(callback: (event: EmitterEvent) => void): void`
Подписка на все события. Когда любое событие срабатывает, вызывается указанный обработчик `(callback)`.
`callback:` Функция, которая будет вызвана для каждого события, получая объект с именем события `(eventName)` и данными `(data)`.

5. `offAll(): void`
Удаляет все подписки на события, очищая хранилище `_events`.

6. `trigger<T extends object>(eventName: string, context?: Partial<T>): (data: T) => void`
Создаёт функцию-генератор события, которая при вызове будет инициировать событие с указанными данными.

`eventName:` Имя события, которое нужно инициировать.
`context:` Дополнительные данные, которые будут объединены с переданными при вызове триггера (необязательный параметр).

**Типы:**
``type EventName = string | RegExp;
type Subscriber = Function;
type EmitterEvent = { eventName: string; data: unknown };``

**Основные методы:**
- `on` — подписка на событие. Если событие ещё не зарегистрировано, оно создаётся.
- `off` — удаляет указанный обработчик `(callback)` из списка подписчиков на событие. Если после удаления подписчика для события не остаётся других подписчиков, само событие будет удалено из карты `_events`. Если переданный обработчик `(callback)` не был найден в списке подписчиков для указанного события, метод не вызывает ошибку и не производит изменений.
- `emit` — инициирует событие с указанным именем и переданными данными. Все подписчики события (или подписчики, соответствующие регулярному выражению) будут уведомлены и получат эти данные. Если переданное событие не зарегистрировано, метод `emit` не генерирует ошибку и просто не вызывает никаких обработчиков. В случае события с именем *, метод вызовет все обработчики, подписанные на все события.
- `onAll` — подписка на все события (полезно для отладки или ведения логов).
- `offAll` — удаляет всех подписчиков для всех событий.
- `trigger` — создаёт функцию для генерации события при вызове. Это удобно для создания автоматически инициируемых событий.

**Основные события:**
- `items:changed` — вызывается при изменении списка доступных товаров.
- `item:selected` — возникает при выборе товара пользователем.
- `preview:changed` — вызывается при изменении товара в предварительном просмотре.
- `basket:changed` — генерируется при изменении содержимого корзины.
- `basket:delete` — вызывается при удалении товара из корзины.
- `item:moved` — генерируется при добавлении товара в корзину.
- `modal:open` — вызывается при открытии модального окна.
- `modal:close` — вызывается при закрытии модального окна.
- `order:opened` — генерируется при переходе к оформлению заказа.
- `input:error` — вызывается при ошибках в форме ввода данных.
- `input:change` — вызывается при изменении значения поля ввода.
- `contacts:submit` — вызывается при отправке формы контактных данных.
- `order:submit` — генерируется при отправке формы заказа.
- `order:success` — вызывается при успешной обработке заказа на сервере.

### Описание классов `Model`
1. `ApiModel` (наследуется от класса Api) — отвечает за взаимодействие с сервером. Этот класс передает и получает данные, связанные с заказами и товарами.

*Методы:*
`getListProductCard()` - Получает список товаров с сервера (массив объектов карточек).
`postOrderLot()` - Отправляет данные о заказе на сервер и получает ответ о его статусе.

2. `BasketModel` — управляет данными корзины, которые поступают от пользователя. Хранит информацию о добавленных товарах и их количестве.
```constructor() {
  this.items = []; // Инициализация массива для хранения элементов корзины
  this.totalPrice = 0; // Изначально общая сумма равна 0
}
```
*Методы:*
`getCounter()` - Возвращает количество товаров в корзине.
`getSumAllProducts()` - Рассчитывает и возвращает общую сумму всех товаров в корзине.
`setSelectedCard(product)` - Добавляет товар в корзину.
`deleteCardToBasket(productId)` - Удаляет товар из корзины по идентификатору.
`clearBasketProducts()` - Очищает корзину, удаляя все товары.

3. `DataModel` — хранит и управляет данными товаров, полученными с сервера.
*Метод:*
`setPreview(product)` - Принимает данные выбранного товара и сохраняет их для отображения подробной информации.

4. `FormModel` — отвечает за хранение и валидацию данных, полученных от пользователя (например, адрес доставки и контактные данные).
*Методы:*
`setOrderAddress(address)` - Сохраняет адрес пользователя для оформления заказа.
`validateOrder()` - Проверяет валидность введенного адреса и выбранного способа оплаты.
`setOrderData(contactInfo)` - Принимает и сохраняет контактные данные пользователя (номер телефона, почту).
`validateContacts()` - Проверяет корректность контактных данных.
`getOrderLot()` - Возвращает объект, содержащий данные пользователя и выбранные товары для заказа.

### Описание классов `View`

## Удаленные классы
В текущей версии проекта были удалены следующие классы:

- **Gallery** — класс, отвечавший за отображение галереи товаров. Он был исключен, так как не использовался в текущей реализации.
- **Header** — класс, управляющий шапкой сайта. Он был удален, поскольку функциональность была перераспределена между другими компонентами.
- **OrderStatusModal** — класс, отвечавший за отображение статуса заказа в модальном окне. Был исключен в пользу более гибкой реализации с использованием других компонентов.

### Классы включенные в реализацию

### Класс `Basket`

**Описание**:  
Класс `Basket` отвечает за отображение и управление корзиной покупок на сайте. Он включает методы для рендера корзины, отображения количества товаров и общей суммы, а также взаимодействует с модальными окнами и событиями на странице.

**Реализует интерфейс**: `IBasket`

#### Конструктор

``constructor(template: HTMLTemplateElement, protected events: IEvents)``
**Параметры:**

``template: HTMLTemplateElement`` — шаблон HTML, используемый для создания корзины.

``events: IEvents`` — объект для работы с системой событий, через который осуществляется обработка действий (например, открытие модальных окон).

**Свойства**
``basket: HTMLElement`` — контейнер корзины (основной элемент, в котором содержатся все компоненты корзины).

``title: HTMLElement`` — элемент заголовка корзины.

``basketList: HTMLElement`` — контейнер для списка товаров в корзине.

``button: HTMLButtonElement`` — кнопка оформления заказа.

``basketPrice: HTMLElement`` — элемент для отображения общей суммы товаров в корзине.

``headerBasketButton: HTMLButtonElement`` — кнопка в хедере для открытия корзины.

``headerBasketCounter: HTMLElement`` — элемент в хедере, показывающий количество товаров в корзине.

**Методы**
``renderHeaderBasketCounter(value: number): void``

**Описание:** Отображает количество товаров в корзине в хедере.

*Параметры:* ``value: number`` — количество товаров, которое будет отображаться.

``renderSumAllProducts(sumAll: number): void``

**Описание:** Отображает общую сумму товаров в корзине.

**Параметры:** ``sumAll: number`` — сумма всех товаров в корзине.

``render(): HTMLElement``

**Описание:** Метод для рендера самого компонента корзины. Устанавливает заголовок корзины и возвращает основной элемент корзины.

Возвращаемое значение: возвращает элемент корзины (HTMLElement).

**Прочее**
``Сеттер items:``

**Описание:** Устанавливает список товаров в корзине. Если товары есть, отображает их в корзине и активирует кнопку. Если корзина пуста, отображает сообщение и блокирует кнопку.

*Параметры:* ``items: HTMLElement[]`` — массив HTML-элементов, представляющих товары в корзине.

**События:**

Класс подписан на два события:
``order:open`` — открытие окна оформления заказа.
``basket:open`` — открытие модального окна корзины.

### Modal
**Назначение:** Базовый класс для отображения модальных окон. Обеспечивает открытие, закрытие и управление состоянием модальных окон.

**Методы:**
- `open(content: HTMLElement): void` - Открывает модальное окно с переданным содержимым.
- `close(): void` - Закрывает модальное окно и очищает содержимое.
- `setState(isBlocked: boolean): void` - Управляет состоянием модального окна. Если `isBlocked` равно `true`, добавляется блокирующий стиль.

## Класс `Order`

### Описание

Класс `Order` представляет модель формы заказа, которая включает выбор способа оплаты, обработку ввода данных и отправку формы. Класс управляет состоянием кнопки отправки, а также взаимодействует с системой событий для изменения данных о заказе и отправки формы.

### Реализует интерфейс

- `IOrder`

### Конструктор

``constructor(template: HTMLTemplateElement, protected events: IEvents)``
*Параметры:*

``template: HTMLTemplateElement`` — шаблон HTML для создания формы заказа.

``events: IEvents`` — объект для работы с системой событий.

*Свойства*
``formOrder: HTMLFormElement`` — форма заказа, которая клонируется из переданного шаблона.

``buttonAll: HTMLButtonElement[]`` — массив всех кнопок выбора метода оплаты в форме.

``buttonSubmit: HTMLButtonElement`` — кнопка для отправки формы.

``formErrors: HTMLElement`` — элемент для отображения ошибок формы.

``paymentSelection: string`` — выбранный метод оплаты (может быть 'online' или 'offline').

*Методы*
``set paymentSelection(paymentMethod: string)``
*Описание:* Устанавливает обводку вокруг выбранного метода оплаты и активирует соответствующую кнопку.

*Параметры:*
``paymentMethod: string`` — выбранный метод оплаты (например, 'online' или 'offline').

``set valid(value: boolean)``
*Описание:* Устанавливает состояние кнопки отправки формы. Если форма не валидна, кнопка будет заблокирована.

*Параметры:*
``value: boolean`` — флаг валидности формы.

``render(): HTMLElement``
*Описание:* Рендерит форму заказа и возвращает её для отображения.

*Возвращаемое значение:*

Возвращает элемент формы заказа (HTMLFormElement), готовый для отображения.

*Прочее*
*События*
``order:paymentSelection`` — генерируется при выборе метода оплаты (например, 'online' или 'offline').

``order:changeAddress`` — генерируется при изменении данных в полях формы (например, адреса).

``contacts:open`` — генерируется при отправке формы заказа для открытия формы с контактными данными.

*Примечания*
Класс активно взаимодействует с системой событий для обновления данных о заказе и перехода между шагами оформления заказа. Он также управляет визуальным отображением выбранного способа оплаты.


### Класс `BasketItems`

 **Описание**

Класс `BasketItems` отвечает за отображение отдельного элемента в корзине. Он используется для рендеринга информации о каждом товаре в корзине, включая название, цену и индекс товара. Также предоставляет возможность добавить обработчик события для удаления товара из корзины.

### Реализует интерфейс

- `IBasketItems`

### Конструктор

``constructor(template: HTMLTemplateElement, protected events: IEvents, actions?: IActions)``
Параметры:

``template: HTMLTemplateElement`` — шаблон HTML для создания элемента товара в корзине.

``events: IEvents`` — объект для работы с системой событий.

``actions?: IActions`` — объект с действиями (например, обработчик клика для кнопки удаления товара).

**Свойства**
``basketItem: HTMLElement`` — контейнер для одного элемента товара в корзине.

``index: HTMLElement`` — элемент для отображения индекса товара.

``title: HTMLElement`` — элемент для отображения названия товара.

``price: HTMLElement`` — элемент для отображения цены товара.

``buttonDelete: HTMLButtonElement`` — кнопка для удаления товара из корзины.

**Методы**
``setPrice(value: number | null): string``
Описание: Устанавливает цену товара. Если цена товара равна null, возвращает строку 'Бесценно', иначе — строку с ценой в синапсах.

**Параметры:**

`value: number | null` — цена товара.

*Возвращаемое значение:*

Возвращает строку с ценой товара.

`render(item: IBasketItem, index: number): HTMLElement`
Описание: Метод рендеринга для компонента BasketItem. Отображает индекс, название и цену товара.

*Параметры:*

``item: IBasketItem`` — объект товара, который отображается в корзине.

`index: number` — индекс товара в корзине.

**Возвращаемое значение:**

Возвращает элемент корзины (HTMLElement), отображающий один товар.

**Прочее**
*События*
При наличии переданных действий (например, обработчика клика), кнопка удаления товара будет подписана на событие click для вызова этого действия.

## Класс `Card`

### Описание

Класс `Card` отвечает за создание и отображение карточки товара. Он рендерит элемент с данными товара, такими как категория, название, изображение и цена. Также включает возможность добавить обработчик события для клика по карточке товара.

### Реализует интерфейс

- `ICard`

### Конструктор

``constructor(template: HTMLTemplateElement, protected events: IEvents, actions?: IActions)``
*Параметры:*

``template: HTMLTemplateElement`` — шаблон HTML для создания карточки товара.

``events: IEvents`` — объект для работы с системой событий.

``actions?: IActions`` — объект с действиями (например, обработчик клика по карточке).

*Свойства*
``_cardElement: HTMLElement ``— основной контейнер карточки товара.

`_cardCategory: HTMLElement` — элемент для отображения категории товара.

``_cardTitle: HTMLElement`` — элемент для отображения названия товара.

`_cardImage: HTMLImageElement` — элемент для отображения изображения товара.

`_cardPrice: HTMLElement` — элемент для отображения цены товара.

`_colors: Record<string, string>` — объект, который маппит категории товара на соответствующие CSS классы.

**Методы**
``setText(element: HTMLElement, value: unknown): string``
Описание: Устанавливает текстовое значение для элемента и возвращает это значение как строку.

Параметры:

``element: HTMLElement`` — элемент, в котором будет установлен текст.

``value: unknown`` — значение, которое нужно отобразить.

Возвращаемое значение:

Возвращает строку с установленным значением.

``set cardCategory(value: string)``
Описание: Устанавливает категорию товара для отображения. Меняет текстовое содержание и CSS класс элемента категории в зависимости от значения.

Параметры:

``value: string`` — категория товара.

``setPrice(value: number | null): string``
Описание: Устанавливает цену товара. Если цена товара равна null, возвращает строку 'Бесценно', иначе — строку с ценой в синапсах.

Параметры:

``value: number | null`` — цена товара.

Возвращаемое значение:

Возвращает строку с ценой товара.

``render(data: IProductItem): HTMLElement``
Описание: Рендерит карточку товара с данными, переданными через параметр data. Устанавливает все необходимые поля для отображения: категорию, название, изображение и цену товара.

Параметры:

``data: IProductItem`` — объект с данными товара для отображения.

Возвращаемое значение:

Возвращает элемент карточки товара (HTMLElement), готовый для отображения.

Прочее
События
При наличии переданных действий (например, обработчика клика), карточка товара будет подписана на событие click для вызова этого действия.

### Класс `CardPreview`

### Описание

Класс `CardPreview` расширяет класс `Card` и добавляет дополнительные элементы и логику для отображения предварительного просмотра карточки товара. Он включает возможность отобразить описание товара, а также кнопку, которая может быть активной или неактивной в зависимости от наличия цены.

### Реализует интерфейс

- `ICard`

### Конструктор


`constructor(template: HTMLTemplateElement, protected events: IEvents, actions?: IActions)`
**Параметры:**

``template: HTMLTemplateElement`` — шаблон HTML для создания карточки товара.

``events: IEvents`` — объект для работы с системой событий.

``actions?: IActions`` — объект с действиями (например, обработчик клика по карточке).

Свойства
``text: HTMLElement`` — элемент для отображения описания товара.

``button: HTMLElement`` — кнопка для добавления товара в корзину или отображения статуса продажи.

**Наследуемые от Card:**

``_cardElement: HTMLElement`` — основной контейнер карточки товара.

``_cardCategory: HTMLElement`` — элемент для отображения категории товара.

``_cardTitle: HTMLElement`` — элемент для отображения названия товара.

``_cardImage: HTMLImageElement`` — элемент для отображения изображения товара.

``_cardPrice: HTMLElement`` — элемент для отображения цены товара.

``_colors: Record<string, string>`` — объект, который маппит категории товара на соответствующие CSS классы.

Методы
``notSale(data: IProductItem): string``
Описание: Проверяет, продается ли товар. Если цена товара присутствует, то кнопка будет активной и отображать текст "Купить". Если цена отсутствует, кнопка будет заблокирована и отображать текст "Не продается".

**Параметры:**

``data: IProductItem`` — объект с данными товара.

*Возвращаемое значение:*

Возвращает строку с текстом для кнопки ("Купить" или "Не продается").

``render(data: IProductItem): HTMLElement``
Описание: Рендерит предварительный просмотр карточки товара с данными, переданными через параметр data. Устанавливает все необходимые поля для отображения: категорию, название, изображение, цену, описание и кнопку.

*Параметры:*

``data: IProductItem`` — объект с данными товара для отображения.

*Возвращаемое значение:*

Возвращает элемент карточки товара (HTMLElement), готовый для отображения.

**Прочее**
**События**
При клике на кнопку карточки будет отправлено событие card:addBasket, уведомляя систему о добавлении товара в корзину.

### Описание Contacts

Класс `Contacts` представляет модель контактных данных, включая форму с полями для ввода и кнопку отправки. Он управляет обработкой изменений в полях формы, отправкой формы и отображением ошибок. Также класс управляет состоянием кнопки отправки формы в зависимости от валидности введённых данных.

### Реализует интерфейс

- `IContacts`

### Конструктор
``constructor(template: HTMLTemplateElement, protected events: IEvents)``

*Параметры:*

``template: HTMLTemplateElement`` — шаблон HTML для создания формы контактов.

``events: IEvents`` — объект для работы с системой событий.

*Свойства*
``formContacts: HTMLFormElement`` — форма контактов, которая клонируется из переданного шаблона.

``inputAll: HTMLInputElement[]`` — массив всех полей ввода в форме.

``buttonSubmit: HTMLButtonElement`` — кнопка для отправки формы.

``formErrors: HTMLElement`` — элемент, содержащий ошибки формы.

*Методы*
``set valid(value: boolean)``
Описание: Устанавливает состояние кнопки отправки формы. Если форма не валидна, кнопка будет заблокирована.

*Параметры:*

``value: boolean`` — флаг валидности формы.

``render(): HTMLElement``
*Описание:* Рендерит форму контактов и возвращает её для отображения.

*Возвращаемое значение:*

Возвращает элемент формы контактов (HTMLFormElement), готовый для отображения.

*Прочее*
*События*
``contacts:changeInput`` — генерируется при изменении поля формы (например, при вводе текста в поле email или phone).

``success:open`` — генерируется при отправке формы, что открывает окно с сообщением об успешной отправке.

*Примечания*
Класс активно взаимодействует с системой событий, чтобы сообщать о изменениях в полях ввода и о завершении отправки формы.

## Класс Success

### Описание

Класс `Success` представляет модель компонента окна с сообщением о успешной операции, например, успешной оплате. Он отображает сумму, списанную с пользователя, и предоставляет возможность закрыть это окно через кнопку.

### Реализует интерфейс

- `ISuccess`

### Конструктор

``constructor(template: HTMLTemplateElement, protected events: IEvents)``
*Параметры:*

``template: HTMLTemplateElement`` — шаблон HTML для создания компонента успешного заказа.

``events: IEvents`` — объект для работы с системой событий.

*Свойства*
``success: HTMLElement`` — основной элемент компонента, представляющий окно успешного заказа.

``description: HTMLElement`` — элемент, отображающий описание (например, сумму списания).

``button: HTMLButtonElement`` — кнопка для закрытия окна успешного заказа.

*Методы*
``render(total: number): HTMLElement``
*Описание:* Рендерит компонент с отображением суммы, списанной с пользователя, и возвращает готовый элемент для отображения.

*Параметры:*

``total: number`` — сумма, которая была списана.

*Возвращаемое значение:*

Возвращает элемент компонента успешного заказа, готовый для отображения.

*Прочее*
*События*
``success:close`` — генерируется при закрытии окна успешного заказа.

*Примечания*
Класс используется для отображения окон с сообщением о успешной оплате или другом успешном действии и включает возможность закрытия этого окна через кнопку.

### Пользовательский сценарий: 
1. Начинаем с каталога товаров. 
2. Кликаем по карточке товара и открывается его подробное описание. 
3. Добавляем товар в корзину с помощью кнопки "Купить". 
4. Возвращаемся в каталог и можем повторить этот процесс несколько раз, добавляя товары в корзину. 
5. Все добавленные товары будут отображаться в корзине. 
6. Кликаем на корзину, чтобы перейти к списку добавленных товаров, где можно изменять количество и удалять товары. 
7. Нажимаем кнопку "Оформить" и переходим к форме для ввода персональных данных. 
8. Заполняем форму с выбором способа оплаты и адресом доставки, а также указываем почту и номер телефона. 
9. После нажатия кнопки "Оплатить" происходит процесс оформления заказа. 
10. На экране появляется уведомление о том, что заказ оформлен, и сумма списана (при успешной оплате). 
